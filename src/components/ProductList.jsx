import React from 'react';

const ProductList = () => {
    // For now, let's add some placeholder text
    return (
        <div className="product-list">
            <h2>Our Products</h2>
            <p>This is where the list of products will go.</p>
        </div>
    );
};

export default ProductList;